name: Deploy web to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "web/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

# 该工作流来自分叉仓库的 GitHub Pages 预览，核心仓库 LingByte/CloudSteps 未启用 Pages。
# 在官方仓库跳过，避免 deploy-pages 404；fork 仍可自行部署。
jobs:
  build:
    if: github.repository != 'LingByte/CloudSteps'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml

      - name: 安装依赖
        working-directory: web
        run: pnpm install --frozen-lockfile

      - name: 构建（项目页子路径 + 后端 API 地址）
        working-directory: web
        env:
          # 仓库名作为 GitHub Pages 项目页子路径，如需自定义域名可改为 "/"
          VITE_BASE_PATH: "/${{ github.event.repository.name }}/"
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
        run: pnpm run build

      - name: SPA 路由回退（history 模式在 GitHub Pages 刷新深层路径会 404）
        working-directory: web
        run: cp dist/index.html dist/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    if: github.repository != 'LingByte/CloudSteps'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
